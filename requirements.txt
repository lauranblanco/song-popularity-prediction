# Runtime (app + pipeline) — versions pinned to match the serialized model
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
lightgbm==4.6.0
shap==0.52.0
joblib==1.5.3
streamlit==1.58.0
matplotlib==3.10.9
seaborn==0.13.2
requests==2.34.2
python-dotenv==1.2.2

# Notebooks only (not needed for the deployed app)
jupyter==1.0.0
